{
  "name": "tiny-triage",
  "version": "1.0.0",
  "description": "A tiny task/issue triage board served by a small Node HTTP server, with zero runtime dependencies.",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/store.js && node --check src/reports.js && node --check public/app.js && node --check public/filter.js && node --check public/counts.js && node --check public/shortcuts.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
